{
  "buildCommand": "pnpm --filter @workspace/school-website build && cp -r artifacts/school-website/dist/public ./public",
  "outputDirectory": "public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
